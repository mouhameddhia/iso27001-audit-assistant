[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: needs a running Qdrant server, Ollama and/or Postgres (skipped when unreachable)
    e2e: runs the real ingestion pipeline against live Ollama and Qdrant (skipped when unreachable)
