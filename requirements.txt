# Core
numpy>=1.26
requests>=2.31

# Configuration and structured output validation
pydantic>=2.5
pydantic-settings>=2.3

# Vector database
qdrant-client>=1.12

# Lexical retrieval (BM25)
rank_bm25>=0.2.2

# Cross-encoder reranking (Stage 2)
sentence-transformers>=3.0
hf_xet>=1.0  # fast model downloads from the Hugging Face Hub

# Testing
pytest>=8.0

# Optional: only needed if you ingest PDF source documents
pypdf>=4.0

# Report generation (Stage 4)
python-docx>=1.1
reportlab>=4.0

# Client document import (XLSX/CSV client evidence)
openpyxl>=3.1

# Development phase: API backend (persistent storage, auth, HTTP layer)
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
psycopg2-binary>=2.9
alembic>=1.13
bcrypt>=4.0
pyjwt>=2.8
python-multipart>=0.0.9  # file uploads (document import)
httpx>=0.27  # FastAPI TestClient
